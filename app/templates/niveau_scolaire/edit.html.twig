{% extends 'base.html.twig' %}

{% block title %}Éditer le Niveau Scolaire{% endblock %}

{% block body %}
<div class="max-w-3xl mx-auto">
    <h1 class="text-4xl font-bold text-neon mb-8">
        {{ block('title') }}
    </h1>

    {{ include('niveau_scolaire/_form.html.twig', {
        'button_label': 'Mettre à jour'
    }) }}

    <a href="{{ path('app_niveau_scolaire_index') }}" class="text-cyan-400 hover:underline mt-6 inline-block">
        <i class="fas fa-arrow-left mr-1"></i>Retour à la liste
    </a>

    <div class="mt-8 p-4 bg-card rounded-lg border border-red-500/20">
        <h2 class="text-red-500 font-bold mb-4">Danger Zone</h2>
        {{ include('niveau_scolaire/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
