<form method="post" action="{{ path('app_niveau_scolaire_delete', {'id': niveau_scolaire.id}) }}" 
      onsubmit="return confirm('Supprimer ce niveau scolaire ?');" class="inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ niveau_scolaire.id) }}">
    <button class="bg-red-600 text-white px-6 py-2 rounded-lg hover:bg-red-500 transition">
        <i class="fas fa-trash mr-2"></i>Supprimer
    </button>
</form>
