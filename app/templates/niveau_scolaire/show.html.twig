{% extends 'base.html.twig' %}

{% block title %}NiveauScolaire{% endblock %}

{% block body %}
    <h1>NiveauScolaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ niveau_scolaire.id }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ niveau_scolaire.niveau }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_niveau_scolaire_index') }}">back to list</a>

    <a href="{{ path('app_niveau_scolaire_edit', {'id': niveau_scolaire.id}) }}">edit</a>

    {{ include('niveau_scolaire/_delete_form.html.twig') }}
{% endblock %}
